📍 MediaTools.New-DefaultGeoTag Function Documentation

Description:
Returns a standard, module-wide string representing a "zero" or invalid geotag. This value is intentionally outside the valid coordinate range and is used as a consistent placeholder when a valid geotag cannot be found or generated.

Input Type:
None.

Output Type:
✅ [string] — The default invalid geotag string (e.g., "200,M,200,M").

Algorithm (Pseudo-code):
1. Return the value of the module-level constant `$script:DefaultZeroGeoTag`.

Function Calls:
None.

Possible Issues:
* ✅ Covered: Provides a single, consistent source for the default invalid geotag by using a module-level constant, preventing "magic string" issues across the module.
* ✅ Covered: The returned value uses an intentionally invalid coordinate (e.g., latitude of 200) to prevent it from being mistaken for a real-world location (such as 0,N,0,E).
