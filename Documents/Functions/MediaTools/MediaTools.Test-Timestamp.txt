🕒 MediaTools.Test-Timestamp Function Documentation

Description:
Checks if a given timestamp string is valid. It does this by attempting to parse the string using the module's canonical `Format-Timestamp` function. A timestamp is considered valid if it can be successfully parsed into a non-zero date.

Input Type:
1. InputTimestamp [string] — The timestamp string to validate.

Output Type:
✅ [bool] — Returns `$true` if the timestamp is valid, otherwise `$false`.

Algorithm (Pseudo-code):
1. If the input string is null or empty, return `$false`.
2. Call `Format-Timestamp` on the input inside a `try/catch` block.
3. If `Format-Timestamp` throws an exception, the timestamp is invalid, so return `$false`.
4. If `Format-Timestamp` returns the module's default "zero timestamp" value, return `$false`.
5. If the function succeeds and does not return a zero-date, return `$true`.

Function Calls:
🔹 Format-Timestamp

Possible Issues:
* ✅ Covered: Leverages the robust, multi-format parsing engine of `Format-Timestamp`.
* ✅ Covered: Correctly identifies the module's default zero-date value as invalid.
* ✅ Covered: Gracefully handles any parsing failures by returning `$false` instead of throwing an exception.

