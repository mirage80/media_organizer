### Write_JsonAtomic Function Documentation

Function:
Write_JsonAtomic

Description:
Safely writes a JSON object to a file using a temporary file and atomic move to prevent partial writes or corruption.

Input Type:

1. JsonObject [object] — Data to serialize and write
2. TargetPath [string] — Destination file path
3. Indent [bool] — Whether to format the JSON with indentation (default: $true)

Output Type:
✅ [void] — JSON file is written successfully
❌ Throws on serialization or file I/O errors unless caught externally

Algorithm (Pseudo-code):

1. Validate Inputs
   1.1 If JsonObject is null → throw
   1.2 If TargetPath is null or empty → throw

2. Determine Temp Path
   2.1 Create $tempPath by appending .tmp to TargetPath

3. Convert to JSON
   3.1 If Indent is true → call ConvertTo-Json -Depth 100 -Compress:$false
   3.2 Else → call ConvertTo-Json -Depth 100 -Compress:$true
   🔹 Purpose: Convert input object to JSON text

4. Write to Temp File
   4.1 Use Set-Content or Out-File to write JSON to $tempPath

5. Atomically Replace Original File
   5.1 Use Move-Item -Force to replace TargetPath with $tempPath

Function Calls:

* ConvertTo-Json → Serializes the object
* Move-Item → Used for atomic replacement
* Set-Content or Out-File → Writes content to temporary file (may be implicit)

Possible Issues:

* ✅ Covered: Indentation option
* ✅ Covered: Atomic write via temp file
* ✅ Covered: Replaces existing file
* ❌ Not Covered: Serialization errors (e.g. cyclic references)
* ❌ Not Covered: Target path directory does not exist
* ❌ Not Covered: File write permission denied
* ❌ Not Covered: Simultaneous write by another process
* ❌ Not Covered: File system full or I/O errors